# Frontend build target for production deploy to Hostinger.
# In dev (npm run dev) this file is ignored; relative /api/* still hits the Vite proxy
# which forwards to the legacy Node server (or Docker app) on localhost:4377/4378.
VITE_API_BASE_URL=https://api-matter.stellarinfomatica.com
